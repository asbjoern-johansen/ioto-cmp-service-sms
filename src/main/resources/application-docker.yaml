spring:
  application:
    name: iot-cmp-services-sms
  main:
    banner-mode: "off"
  profiles:
    active: "docker"

server:
  port: 9006

#Actuator
management:
  server:
    port: 9007  # different port for actuator check
  endpoints:
    web:
      exposure:
        include: health,info,beans,mappings   # only exposes these endpoints
  endpoint:
    health:
      show-details: always

ioto:
  cmp:
    service:
      sms:
        smpp:
          host: smsc
          port: 2775
          system-id: smppclient1
          password: password
          system-type: cp
          service-type: CMT
          reconnect-delay-ms: 5000
          source-number: ioto
          source-ton: INTERNATIONAL
          source-npi: ISDN
          dest-ton: INTERNATIONAL
          dest-npi: ISDN





